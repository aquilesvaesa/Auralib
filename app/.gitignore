.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
pubspec.lock
.metadata

# Code-gen
*.freezed.dart
*.g.dart

# IDE
.idea/
.vscode/
*.iml

# Android
android/.gradle/
android/local.properties
android/captures/
android/key.properties
android/app/release/
android/app/debug/
android/app/profile/
android/app/google-services.json

# Firebase: google-services.json es local (ver arriba).
# `lib/firebase_options.dart` en el repo debe ser solo la plantilla (sin claves).
# Genera valores reales en tu máquina con `dart run tool/sync_firebase_options.dart` y no hagas
# commit de ese archivo si contiene API keys reales (ver README sección Seguridad).
.DS_Store
